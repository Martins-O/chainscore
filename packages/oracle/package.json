{
  "name": "@chainscore/oracle",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "start": "node dist/index.js",
    "dev": "npx tsx src/index.ts",
    "dry-run": "npx tsx src/index.ts --dry-run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "node-cron": "^3.0.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
